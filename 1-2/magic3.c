#include<stdio.h>
int main(){

   int n,s,d,x,y,c;

   scanf("%d %d %d", &n, &s, &d);

   while(n--){
   scanf("%d %d", &x, &y);
   if(x<s && y>d){
   c=1;

   }

   }
   if(c==1){
   printf("Yes");
   }

   else{
   printf("No");

   }

   return 0;

}
