#include<stdio.h>
int main()
{
    int t,n,a,b,c,d;
    scanf("%d", &t);
    while(t--)
    {
        scanf("%d", &n);
       a=n-3;
       b=1;
       c=1;
       d=1;
        printf("%d %d %d %d\n", a,b,c,d );
    }
    return 0;
}
